/**
 * BizCity Tool Image — Admin Dashboard JS
 * @since 2.0.0
 */
(function($) {
    'use strict';
    // Admin dashboard — minimal JS placeholder
    // Main UI logic is inline in page-image-profile.php
})(jQuery);
